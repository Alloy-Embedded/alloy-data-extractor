"""Normalization helpers — currently empty.

The CMSIS-SVD extractor emits canonical-IR-shaped payloads
directly.  Future vendor-specific normalizers (consolidating
the per-family ``_build_*_device_ir`` builders that live in
alloy-codegen today) land here as adapters migrate.
"""
